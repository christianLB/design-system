import { Meta, Story, Preview } from '@storybook/addon-docs';
import Button from './Button';

<Meta title="Components/Button" component={Button} />

# Button

The `Button` component triggers an action or event.

<Preview>
  <Story name="Primary"><Button variant="primary">Primary</Button></Story>
</Preview>
